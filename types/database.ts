/**
 * Supabase database types (Phase 0A placeholder).
 *
 * The full Vurlo schema (links, clicks, profiles, etc.) belongs to a later
 * phase. This file exists so `lib/supabase/*` has a clean, typed home for
 * generated types when the schema lands.
 *
 * When the schema is ready, replace this with the output of:
 *   supabase gen types typescript --project-id <id> > types/database.ts
 */
export type Database = Record<string, never>;
